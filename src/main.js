/**
 * RAM-Stream-Browser — Frontend Entry
 * Sprint 1: Minimal shell that navigates to B站.
 * The real work happens in inject/preload.js (injected via Tauri initialization_script).
 */

// Re-export for module use
export {};
